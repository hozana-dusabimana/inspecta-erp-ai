# INSPECTA BUILDOS — Frontend environment
# Copy to ".env" (Vite reads VITE_* at build/dev time).

# Base URL of the backend API. Default matches the backend's default port.
VITE_API_URL=http://localhost:4000/api

# Port the frontend dev/host server listens on (server.ts).
PORT=3000
